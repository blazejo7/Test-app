import { ScreenStub } from '@/components/screen-stub';

export default function Rota() {
  return (
    <ScreenStub
      title="Rota"
      description="Leads mark availability for the next 2 days; manager assigns, publishes, and confirms receipt."
      phase="Roadmap · Feature 8"
    />
  );
}
