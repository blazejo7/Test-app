import { ScreenStub } from '@/components/screen-stub';

export default function SignOff() {
  return (
    <ScreenStub
      title="Sign-off queue"
      description="Review grounded vans and approve their release so they can go back out."
      phase="Roadmap · Feature 6"
    />
  );
}
