import { ScreenStub } from '@/components/screen-stub';

export default function Inspect() {
  return (
    <ScreenStub
      title="Inspection"
      description="Claim a van → confirm known damage → zone-by-zone walkaround → log new damage + photos → fluids → review → submit."
      phase="Roadmap · Feature 3"
    />
  );
}
