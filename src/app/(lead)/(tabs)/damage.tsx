import { ScreenStub } from '@/components/screen-stub';

export default function Damage() {
  return (
    <ScreenStub
      title="Damage log"
      description="New vs known damage views, photo gallery, and full damage history per van."
      phase="Roadmap · Feature 5"
    />
  );
}
